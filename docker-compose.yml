version: '3.8'

services:
  backend:
    build: .
    container_name: webscan-backend
    ports:
      - "5000:5000"
    volumes:
      - ./:/app
    environment:
      - FLASK_ENV=development
    restart: unless-stopped
